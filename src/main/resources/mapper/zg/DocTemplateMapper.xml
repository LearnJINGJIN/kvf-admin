<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kalvin.kvf.modules.zg.mapper.DocTemplateMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.kalvin.kvf.modules.zg.entity.DocTemplate">
        <id column="id" property="id" />
        <result column="doc_name" property="docName" />
        <result column="doc_addr" property="docAddr" />
        <result column="stage_status" property="stageStatus" />
        <result column="remarks" property="remarks" />
        <result column="dr" property="dr" />
        <result column="create_user" property="createUser" />
        <result column="create_date" property="createDate" />
        <result column="update_user" property="updateUser" />
        <result column="update_date" property="updateDate" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
       id, doc_name, doc_addr, stage_status, remarks, dr, create_user, create_date, update_user, update_date
    </sql>

    <select id="selectDocTemplateList" resultType="com.kalvin.kvf.modules.zg.entity.DocTemplate">
        select <include refid="Base_Column_List" /> from zg_doc_template
        <trim prefix="where" prefixOverrides="and">
            <if test="docTemplate.docName != null and docTemplate.docName != ''">
                and doc_name like concat(#{docTemplate.docName}, "%")
            </if>
        </trim>
    </select>

</mapper>
